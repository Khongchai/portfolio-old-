query Projects(
  $skip: Int!
  $limit: Int!
  $sortBy: String
  $order: String
  $search: String
) {
  projects(
    input: {
      skip: $skip
      limit: $limit
      sortBy: $sortBy
      order: $order
      search: $search
    }
  ) {
    projects {
      ...ProjectFields
    }
    isFirstQuery
    isLastQuery
  }
}
