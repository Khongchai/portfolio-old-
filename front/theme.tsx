import { extendTheme } from "@chakra-ui/react";

const colors = {
  mainGrey: "#545161",
};

const fonts = {
  body: "Selawik",
};

const theme = extendTheme({ colors, fonts });
export default theme;
